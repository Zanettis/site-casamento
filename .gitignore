# Vídeo original (não comprimido) do hero — grande demais para o git.
# A versão usada no site (comprimida) fica em assets/video/hero.mp4 e é versionada normalmente.
assets/video/hero-original.mp4
assets/video/hero-previous.mp4
.gstack/
